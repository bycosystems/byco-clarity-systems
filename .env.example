# Copie ce fichier en ".env" (jamais commité, dans .gitignore).

# Clé PUBLIQUE Vapi (dashboard.vapi.ai -> Settings -> API Keys -> Public
# Key). Faite pour être exposée côté navigateur, comme une clé publiable
# Stripe -- ne jamais y mettre la clé privée.
VITE_VAPI_PUBLIC_KEY=
